apiVersion: apps/v1
kind: Deployment
metadata:
  name: program-service
  namespace: newevent
spec:
  replicas: 2
  selector:
    matchLabels:
      app: program-service
  template:
    metadata:
      labels:
        app: program-service
    spec:
      containers:
        - name: program-service
          image: program-service:v2
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: ConnectionStrings__ProgramDb
              valueFrom:
                secretKeyRef:
                  name: program-db-secret
                  key: ConnectionStrings__ProgramDb
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "150m"
              memory: "128Mi"
          readinessProbe:
            httpGet:
              path: /api/programs
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /api/programs
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 10
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: program-service
  namespace: newevent
spec:
  type: ClusterIP
  selector:
    app: program-service
  ports:
    - port: 80
      targetPort: 8080